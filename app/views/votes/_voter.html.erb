<% if current_user %>
  <div class="vote-arrows pull-left">
    <div><%= link_to image_tag('heart.png'), post_up_vote_path(post), method: :post %></div>
    <div><strong><%= post.points %></strong></div>
  </div>
<% end %>
