<div id="nav" class="navbar-fixed-top" style="background-color:#00BCD4">
  <ul>
    <div id="nav-left">
      <div id="left-title">
        <% if current_user %>
          <li class="logo-font"><%= link_to 'SayIt', topics_path, style: 'color:white;text-transform: uppercase;font-size:2.4em;margin-right:30px;' %></li>
        <% else %>
          <li class="logo-font"><%= link_to 'SayIt', root_path, style: 'color:white;text-transform: uppercase;font-size:2.4em;margin-right:30px;' %></li>
        <% end %>
      </div>
      <div id="left-menu">
        <li><%= link_to 'Topics', topics_path, style: 'color:white' %></li>
        <li><%= link_to 'Categories', topics_path, style: 'color:white' %></li>
      </div>
    </div>
    <div class="nav-right">
        <% if current_user %>
          <li>
            <%= image_tag current_user.avatar_url(48), class: 'gravatar' %>
          </li>
          <li>
            <%= link_to current_user.name, user_path(current_user) %>
          </li>
          <% if user_is_authorized_for_topics? %>
            <li style="color:white;font-weight:bold"> | </li>
            <li id="add-topic">
              <%= image_tag('plus.png') %>
              <%= link_to 'New Topic', new_topic_path %>
            </li>
          <% end %>
          <li style="color:white;font-weight:bold"> | </li>
          <li><%= link_to 'Sign Out', session_path(current_user), method: :delete %></li>
        <% else %>
          <li id="signin"><%= link_to 'Sign In', new_session_path %></li>
          <li><%= link_to 'Sign Up', new_user_path %></li>
        <% end %>
    </div>
  </ul>

</div>
